// extend BT to have recursive search methods. 
// 

class BST extends BT {
	depthFirst(targetValue) {
		// use a depth first search to find targetValue
	}
	breadthFirst(targetValue) {
		// use a breadth first search to find targetValue
	}
}

