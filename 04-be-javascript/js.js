/*

{} to represent an execution context
if a property is an object, put it on stack
use data types from last week
the demo will be like loupe but simpler

object stores property pointing to parent, for scope

types - no objects because objects are js.  purely fp


*/