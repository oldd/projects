// remember's who's turn it is
var nextPlayer = true;

// store current game state
var boardState = [
		[null, null, null],
		[null, null, null],
		[null, null, null]
	];

// it allows players to play.  manages the whole game
function play(box) {
	/*
		determine if DOM element is null. 
		if it is empty, 
			play that player's move. 
		else 
			alert
		advance to next player
	*/
	return undefined;
};

// so players can see what they've done
function populateBoard() {
	/*
		writes the boardState into the DOM table 
		(game board)  
	*/
	return undefined;
};